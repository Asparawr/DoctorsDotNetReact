@model doctors.Models.UserModel

@{
    ViewData["Title"] = "This email is already in use";
}

<h1>This email is already in use</h1>

<form asp-action="Index">
    <div class="form-group">
        <input type="submit" value="Go Back" class="btn btn-primary" />
    </div>
</form>